<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ClientObserver.Views.ServerConnectionSetup.ClientConnectionViews.VideoStreamConnectionView"
             Title="Video Stream Connection Setup">
        <ContentPage.Resources>

    </ContentPage.Resources>
    <VerticalStackLayout>
        <Button Text="Connect to VideoStream" Command="{Binding ConnectCommand}" />

<Label Text="{Binding IsConnected, Converter={StaticResource BoolToStatusText}, StringFormat='Status: {0}'}" />
         
    </VerticalStackLayout>
</ContentPage>
